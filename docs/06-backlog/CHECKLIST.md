# Backlog checklist — items to land gradually

> Purpose: one row per item the Director or the tribunal has deferred, so nothing is lost and
> nothing is re-debated. Each item is processed in its own session or debate, in the order the
> Director decides. A row is closed only with a pointer (debate id, ADR, commit or Director note).
>
> Rules: one item per row; never delete a row, mark it `done` or `dropped` with the pointer;
> the phase column follows the work plan agreed in debate `bus-v2-landing-architecture-001`
> (F0 landing … F6 packaging; F7+ are post-v1 tracks).

Status legend: `open` · `in-debate` · `decided` · `done` · `dropped`

| # | Item | Origin | Phase | Status | Pointer |
|---|------|--------|-------|--------|---------|
| B-01 | **Group referee role** (Director: "árbitro / coordinador" per Telegram group). One optional referee per group, with an explicit rule set: registers tickets with mandatory labels (raising agent, date, status, repo version vs local/VPS version), homologates state, blocks duplicates, re-work and re-debate. Precedent: `FRISCO/frisco-erp/coordinador` (mechanical gate, writes nothing, authorizes nothing, own ledger outside the tree). Runs as a satellite package, never inside the bus core (Alpha objection n1, ADR-06). | Director 2026-09-15 | F7 | open | dedicated debate `bus-v2-referee-001` |
| B-02 | **Skill templates** shipped with the product: Markdown prompts (referee, auditor, developer, documenter…) placed where the control panel can offer them per bot/group for one-click adoption by any agent host. | Director 2026-09-15 | F7 | open | same debate as B-01 |
| B-03 | **Ticket ledger location**: daemon-owned SQLite table exposed as MCP tools (`ticket_open`, `ticket_find`, `ticket_status`) vs external adapters (Odoo, GitHub Issues, Linear). Decide after B-01. | Director 2026-09-15 | F7 | open | — |
| B-04 | **Desktop shell** (system-tray app) for Windows first, macOS second, wrapping the SAME daemon and the SAME local web panel (no second UI). Candidates: Tauri v2 (small, native tray, autostart plugin, Node daemon as sidecar, needs Rust toolchain) vs Electron (JS-only, heavier). Signing budget: Azure Trusted Signing + Apple Developer ID. | Director 2026-09-15 | F8 | open | amendment A2 in `bus-v2-landing-architecture-001` |
| B-05 | **Study gentle-ai installer** (`Gentleman-Programming/gentle-ai`, Go binary, `install --agent <list> --scope global\|workspace`) in a dedicated exploration session: its per-agent adapters (detection paths, config merge semantics, TUI flow). Goal: validate/complete our 15-tool MCP config matrix, reuse knowledge, not code. | Director 2026-09-15 | F0 spike | open | — |
| B-06 | **Headless runner satellite package** (invokes `claude -p`, `codex exec`, `opencode run`, `gemini -p` on new `needs_action`). Out of the core by ADR-06 layers 1–2; own constitution and package; read/reply-only default. First consumer: B-01. | Alpha objection n1 | post-F6 | decided | `bus-v2-landing-architecture-001` round 2 |
| B-07 | **Spike**: bot-to-bot group visibility for a NON-admin bot with privacy disabled — AND vs OR semantics (two official Telegram pages disagree). Determines whether project bots must ever be admins. | critic gap | F0 | done | **Closed 2026-09-19** by official Telegram Bot API documentation: Bot-to-Bot Communication Mode in @BotFather + privacy mode disabled allows non-admin bot to receive all bot messages in group; bots do NOT need to be admin |
| B-08 | **Spike**: IPC handshake on Windows 11 — challenge-response over the per-boot secret before any bearer is sent (Alpha objection n2), plus whether a Node-created named pipe is reachable by another local user. | Alpha objection n2 | F0 | open | — |
| B-09 | **Spike**: per host (Cursor, OpenCode, Codex, Gemini CLI, Antigravity, Claude Code) whether any MCP server-initiated notification reaches the model or triggers a turn. Decides what "timely attention" can honestly promise. | critic gap | F0 | open | — |
| B-10 | **N-party tribunal over the bus** (proposer + judges on different models): needs AGENTBUS/3 or Arena Orion integration; v1 ships 2-party Arena-light only. | proposal D7 | post-F6 | open | — |
| B-11 | **Name clearance**: IMPI + EUIPO screening for "Conmuta" (collision: Conmuta Soluciones Tecnológicas S.L., class 42); fallback "Emisario". Reserve npm scope, GitHub org and domain the same day the Director decides. | proposal D8 | F0 | open | Name decided: Conmuta (DN-02, 2026-09-16); screening and reservations still open |
| B-12 | **macOS smoke test** (installer + daemon + LaunchAgent + one IDE) before claiming dual-platform support. | proposal D9 | F6 | open | — |
| B-13 | **Migration runbook from `~/.agentbus`** (FRISCO team on v1.0.1/v1.0.2): backup, synthesized registry, wire-compat policy (emit AGENTBUS/2, accept /1 and /2). | proposal D1 | F1 | done | **Closed 2026-09-26** (PR-42 close-out sweep): PR-36 (read-only v1 loaders), PR-37 (synthesis + non-interactive `migrate-v1` CLI) and PR-38 (integration test + runbook `docs/runbooks/migrate-from-v1.md`) all merged — unit 11 `v1-migration` is closed |
| B-14 | **Version observability without autonomous emission**: no heartbeat timer (ADR-06 layer 2); build/wire version rides on the render-only header of posts the agent sends anyway. | Kairo self-correction | F3 | decided | amendment A1 |
| B-15 | **Secrets storage verification**: `@napi-rs/keyring` prebuilds on win-x64 and darwin-arm64; fallback file ACL (`icacls` / `chmod 600`) with tests on both platforms. | proposal D4 | F1 | done | **Closed 2026-09-26** (PR-42 close-out sweep) for its F1/win-x64 scope: PR-14 merged (`src/secret-store/{types,keyring,file-fallback,redaction,index}.ts`, real Credential Manager round trip on win-x64, ACL'd fallback file, PT-09/PT-19). The darwin-arm64 half is out of F1 scope and stays tracked separately as **B-12**. |
| B-16 | **Repository hygiene for open source**: LICENSE (Apache-2.0, Director to confirm), SECURITY.md (five invariants), CONTRIBUTING.md, CHANGELOG.md, secret scan in release checklist. | proposal D10 | F0 | open | LICENSE done (Apache-2.0, DN-04, 2026-09-16); SECURITY/CONTRIBUTING/CHANGELOG and the copyright-holder line still open. PT-22 (`test/security/repo-scan.test.ts`, PR-01a) proves the mechanism with synthetic markers only — a real tenant deny-list cannot live in a public tree and must come from outside it (CI secret); decide at the PR-42 close-out (`bus-v2-f1-pr-01-001` item 7) |
| B-17 | **Node ≥ 24 gate** as the first check of the installer, before touching disk or git, with an explicit error and download link (Alpha objection n3). | Alpha objection n3 | F2 | decided | `bus-v2-landing-architecture-001` round 2 |
| B-18 | **Committed project file** `conmuta.json` at repository root, strict zod schema, identifiers only, no local paths, `schema_version` (Alpha objection n4). File name follows the final product name. | Alpha objection n4 | F1 | done | **Closed 2026-09-26** (PR-42 close-out sweep): `bus-v2-landing-architecture-001` round 2 decided it; shipped in PR-08a as `src/shared/project-file.ts` (`z.strictObject` schema per DATA-MODEL §1, `PROJECT_ID_PATTERN`, path-separator/`Authorization` rejection), PT-06 |
| B-19 | **`shared/constants.ts` pins a blind-stripped hash**: its header carries `4ce5e514…` for `v1:src/config.ts:26-166`, where the rule (`bus-v2-f1-pr-04-001`) requires the value **with** the terminating newline, `039d53a2…`. No gate can see it: for a SEAM the provenance scanner asserts only *inequality* against the pin. | PR-07a Judgment Day audit, re-confirmed by PR-07b's (never debated; `bus-v2-f1-pr-07a-audit-001`) | F1 | done | **Closed 2026-09-17** by the post-merge integrity sweep under the Director's session-10 delegation: `constants.ts:3` re-pinned to `039d53a2…` with `4ce5e514…` recorded as the strip control, PR-04's wrong-value-control conclusion corrected in place and its frozen S1 row left as recorded with a closure note. Two methods, validated against the ratified fence pin. See `apply-progress.md` §B-19 and `bus-v2-f1-b19-repin-001` |
| B-20 | **design §12's reuse table marks line-range extracts AS-IS over modules that are SEAM by construction**: rows 462/467 (`send.ts:47-109`, `index.ts:29-42` → `shared/tool-schemas.ts`, reported by PR-07a) and row 458 (`fetch.ts:65-348` → `shared/tool-output.ts`, reported by PR-07b). The tribunal ruling `bus-v2-f1-tasks-001` items 1–2 settles that such a module is SEAM, and the registry proves it independently — an AS-IS body must hash *equal* to its pin, which a re-authored header, an `export` or an added declaration makes impossible. | PR-07a and PR-07b Judgment Day audits (`bus-v2-f1-pr-07a-audit-001`, `bus-v2-f1-pr-07b-audit-001`) | F1 | done | **Closed 2026-09-17** by an *appended* apply-time amendment in design §12 (the audited rows are untouched, so both what was designed and what was shipped remain on the record), applied under the Director's session-10 delegation. Authority: `bus-v2-f1-tasks-001` items 1–2 + `tasks.md` 7a.2/7b.2. Revertible: the amendment note is the only part to revert if the Director prefers to re-open the mapping |
| B-21 | **Advisory finding `R3-tool-output-shape`** from the ordinary native review that closed PR-07b's candidate (lineage `review-7a57283629321227`, lens `review-reliability`): severity WARNING, disposition **informational**, located at `test/shared/tool-output.test.ts:119-121`, the declared-key-set pin for `FetchToolOutput`. The review's closure declared every finding it listed non-blocking — none opened a correction, none reopens that review, and no correction transition was offered for that candidate. The reviewer's rationale is not carried in the closure envelope, so the location is the whole of what was recorded. | Ordinary native review, PR-07b (`gentle-ai.review-acknowledged/v1`, authority burned) | F1 | decided | **Examined 2026-09-17, no change applied.** The pin at 119-121 is the *declared* eighteen-key set, which a runtime fixture cannot witness; the **mandatory** fifteen already have a runtime witness two cases below (`mandatory.length === 15` and `Object.keys(FULL_OUTPUT)`, lines 190-191), which fails if the fixture's keys drift. The two are complements, not a gap, so adding a third witness would be duplication — and the closure forbids re-running review on that candidate. Reversible if the Director reads the finding differently |
| B-22 | **Four advisory findings from the ordinary native review that closed PR-08a's candidate** (lineage `review-f644a39f445a2a0c`, lens `review-reliability`, risk tier medium, authority burned): `R3-01` WARNING at `src/shared/project-file.ts:294-296`; `R3-02` WARNING at `test/shared/project-file.test.ts:5-9`; `R3-03` SUGGESTION at `test/shared/project-file.test.ts:352-362`; `R3-04` SUGGESTION at `src/shared/project-file.ts:151-158`. The closure's own text declares every finding it listed non-blocking: none opened a correction, none reopens that review, and no correction transition is offered for that candidate. The reviewer's rationale is not carried in the closure envelope, so the locations are the whole of what was recorded. | Ordinary native review, PR-08a (`gentle-ai.review-acknowledged/v1`, authority burned) | F1 | open | Recorded 2026-09-17. Not actioned: none is blocking and the closure forbids re-running review on that candidate. Reversible — each location can be examined on its own terms and corrected in a later slice if the finding holds; the review outcome is informational and never grants or withholds delivery |
| B-23 | **`JD-A-003` (Judgment Day SUGGESTION, PR-08a): the deliberate precedence of `unsupported_schema_version` over the content walk is unpinned.** A future-version document that also carries a forbidden shape reports only the version problem, so an operator fixing an upgrade learns nothing about the leak in the same file. The file is still refused, so no secret escapes — the gap is information only. The judge offered two dispositions: report both problem classes, or pin the deliberate precedence with a test that can fail (ADR-12). | PR-08a Judgment Day audit, `jd-judge-a` informational row — **not** part of the authorized severe batch, and not among the four rows the Director folded | F1 | open | Recorded 2026-09-17 in `apply-progress.md` §PR-08a. The behaviour is already documented in `parseProjectFile`'s own doc comment; what is missing is a test that fails if it ever changes. Reversible: either disposition is one small change, and neither reopens PR-08a |
| B-24 | **No POSIX CI leg, so packaging and execution contracts cannot fail here.** `.github/workflows/ci.yml` runs `windows-latest` only, where npm's shim invokes `node` explicitly; a `bin` shebang, file modes and bin-links are therefore invisible to every gate. PR-08b's `JD-B-001` (CRITICAL) was exactly that defect in `src/cli/main.ts`, and ADR-0012 records its failure mode as a silent zero-output success that a pre-commit hook reads as "clean". | PR-08b Judgment Day audit (`JD-B-001`, CRITICAL; ADR-0012 remediation row 2) | F1 | open | Recorded 2026-09-17. The slice added a targeted assertion over the built bundle, which pins the shebang itself; what remains open is the general class. Candidate fix: a POSIX matrix entry, best decided together with the pending `actions/*` major bump. Reversible and cheap |
| B-25 | **Three cheap gates/hygiene items, all for one later audited PR**: (a) **no gate validates the YAML in this tree** — a broken `state.yaml` stayed unparseable for several sessions while `gentle-ai sdd-status` kept working, so a validity check over `git ls-files '*.y*ml'` (session 11 parsed it with PyYAML by hand) would close the class; (b) `npm test` runs whatever is in a stale `dist/`, so a `clean` step would stop a stale build faking results; (c) `actions/checkout@v4` and `actions/setup-node@v4` target Node 20 and emit a deprecation warning. | Hygiene candidates recorded across sessions 9–11; none is a defect report | F1 | open | Recorded 2026-09-17. Deliberately grouped: each is a few lines, each wants the same audited CI/tooling PR, and none belongs inside a feature slice. Reversible and cheap |
| B-26 | **PT-25's owner is attributed differently by two gated documents.** `tasks.md`'s PR-09 block lists `project-binding › Machine registry schema and invariants` **(PT-18, PT-25)** and task 9.6 tells PR-09 to fill PT-25's cell, while `design.md:551` (the PT-to-file mapping) sends **PT-25 to `daemon/send/send-path`**. Both readings are defensible — the row's assertion ("binding is never rewritten from bus or API data") is registry-side R6 *and* the daemon-side `GroupMigratedError` behaviour — and the same class of over-claim cost PR-06a a reverted PT-14 cell. | Found during the session-11 coherence pass over `HANDOFF.md` (reported, not silently resolved) | F1 | open | Recorded 2026-09-17. The next slice fills only the half it really pins and says so in its PR; the remaining half goes to the daemon slice. Reversible: either document can be amended by an appended note, the way B-20 was. **Apply-time note (2026-09-17):** PR-09a filled only **PT-18**'s cell and left PT-25's alone, because the half it pins is the loader's; **PR-09b** names that half (the loader exposes no write path and never renames the human's file) and states the split with `daemon/send/send-path` in its PR, per this row's own prescription |
| B-27 | **The shared token regex matches this project's own roster hash, and R5's raw-text scan therefore refuses every valid registry.** `TELEGRAM_BOT_TOKEN_RE` is `\d+:[A-Za-z0-9_-]{35}` with an **unbounded** digit run, and `ROSTER_HASH_PREFIX` is `sha256:` — a prefix that ends in a digit run — so a canonical `sha256:<64 hex>` value matches the token shape by accident. Design §4 requires the registry's R5 scan to run over the raw text *before* parsing, and `roster_hash` is required on every binding: the two together would refuse the machine registry of every correctly configured machine. PR-09b masks that one value before scanning (`sha256:` plus 64 hexadecimal characters, a region a token cannot occupy because its own colon is outside the hexadecimal class) and pins both halves with tests. | Discovered by PR-09a while writing PR-09b's loader tests (the contradiction only becomes visible once both modules exist) | F1 | open | Recorded 2026-09-17. The mask is a **workaround at the call site**, not a fix: the root cause is the v1-inherited unbounded `\d+`, which also reaches `checkForSecrets` (the outbound backstop would refuse a body quoting a roster hash) and `redactTokenShapes` (PR-14 would redact one in a log). Bounding the digit run to PT-22's `\d{8,10}` is *not* available as a silent fix: `test/shared/token-shape.test.ts` deliberately pins a **7-digit** fixture as a match, because the shared regex is the v1 backstop whose job is to over-reject. So this needs its own decision (widen PT-22's strict shape everywhere, or give the hash a prefix that cannot be read as a token — a stored-format change owned by `design.md` §2 and DATA-MODEL §2.4). Also carries the R5 half: the loader cannot name *which* rule fired without either parsing `assertNoTokenShape`'s message prose or re-implementing the shape order, so naming it is part of this decision. Reversible |
| B-28 | **No R1–R6 row demands referential integrity, so a dangling registry reference loads.** `bindings[].bot_id`, `group_id` and `project_id` name entries of `bots[]`, `groups[]` and `projects[]` (DATA-MODEL §2.4 records those as their source of truth), but no invariant row requires the entry to exist, and `parseRegistryDocument` therefore accepts a binding whose bot or group is absent. The daemon fails closed later — a binding with no `bots[]` entry has no token to resolve, and the room guard compares against `conmuta.json` (R4, at `POST /session`) — but an operator gets no signal at load time. PR-09a pins the boundary with a test instead of inventing a rule the gate does not carry; adding one would make a hand-edited file invalid for a reason no gated document states. | Discovery recorded while implementing PR-09a's `invariants.ts` against DATA-MODEL §2.4 and the R1–R6 table (design §4) | F1 | open | Recorded 2026-09-17. Owner: F2's `doctor` is the natural home — a membership check belongs where the machine is inspected, not where the file is parsed for the daemon. Reversible: if the Director prefers a load-time refusal, it is one `superRefine` rule plus its own row in the invariant table, which would be a design amendment rather than an apply-time edit |
| B-29 | **Nothing at load time ties `roster_hash` to the snapshot it is derived from.** DATA-MODEL §2.4 records the field as *derived* from `bindings[].roster_snapshot`, and design §4 says the client sends its own computation at `POST /session` so a mismatch raises `roster_drift` — but no R1–R6 row requires the stored value to describe the roster the binding admits from, and `registry/schema.ts` checks only its `sha256:<64 hex>` shape. A hand-edited registry (the R6-sanctioned path until F2) can therefore persist a fingerprint belonging to no roster and load clean, and that unverified value is what the daemon returns as `binding.roster_hash` in the session response while the drift comparison the spec defines is derived from the snapshot. Both judges reached this independently in PR-09a's Judgment Day round 1 (`JD-A-001`, `JD-B-002`); the slice fixed the fixture that made it visible (it had pinned the two-entry known answer into a one-entry snapshot) and derives the field with `computeRosterHash` so a fixture cannot disagree with its own snapshot, but the boundary itself is unchanged. | PR-09a Judgment Day round 1, two independent rows | F1 | open | Recorded 2026-09-17. Two dispositions, one decision: a load-time consistency check (which is a *new* invariant rule the gate does not carry, so it needs its own row in the R-table and an amendment), or a session handshake that recomputes the hash from the snapshot instead of trusting the stored field (a `daemon/serve` change, no schema change). The second is smaller and removes the dead-but-trusted value rather than validating it. Reversible |
| B-30 | **R5's strictness refuses human-authored text in the machine registry.** The loader runs `assertNoTokenShape` over the raw file — the whole shared secret table plus the case-insensitive `Authorization` literal — while `registry.json` carries free-form values a human writes: a group `title` of `Authorization review` or `Budget API_KEY=123` refuses the **whole** registry, and the daemon then activates no binding until the text is edited. R5's own wording is narrower ("no token-shaped string anywhere in the file"), but `shared/token-shape.ts` deliberately designates the stricter API for this call site, and this slice's Judgment Day round 1 found the *cost* on a human-editable file (`JD-A-003`, WARNING, single judge). | PR-09b Judgment Day round 1 | F1 | open | Recorded 2026-09-17. Three dispositions, one decision: scan with the narrower token-shape rules for this file; keep the strictness and surface it where the human will see it (the F2 `doctor` and the migration runbook, which already tells operators they may hand-edit the registry); or scope the literal/`.env` rules to identifier-like fields (`token_ref.account`, paths) instead of every string. The daemon's refusal is fail-closed either way, so this is an availability and diagnosis question, not a leak. Reversible |
| B-31 | **R5's raw-text scan is bypassed by a JSON escape.** The scan runs over the raw text *before* parsing (design §4), so a token written as `1234567\u003aAAHk…` has no literal `\d+:[A-Za-z0-9_-]{35}` in the file and is accepted — while the parsed document's value holds the token, which then sits in `~/.conmuta/registry.json` and in the daemon's memory and is reported by nothing. PR-09b's Judgment Day round 1 reached it with a reproduction (`JD-B-006`, SUGGESTION). | PR-09b Judgment Day round 1 | F1 | open | Recorded 2026-09-17. Dispositions: accept the raw-text approximation and say so (the escape only appears if an operator or a tool writes it that way), or add a post-parse value walk beside the raw scan — which is what `shared/project-file.ts` already does for `conmuta.json`, so the machinery exists and the vocabulary would need no new member. The second is more code in the loader; the first leaves a token that nothing reports. Reversible |
| B-32 | **Four advisory findings from the ordinary native review that closed PR-09b's candidate** (lineage `review-c5a6b9c191154861`, lens `review-reliability`, risk tier medium, authority burned): `R3-1` SUGGESTION at `src/registry/loader.ts:109-113`; `R3-2` SUGGESTION at `src/registry/loader.ts:221-229`; `R3-3` SUGGESTION at `src/registry/loader.ts:241-244`; `R3-4` SUGGESTION at `test/registry/loader.test.ts:184-185`. The closure's own text declares every finding non-blocking: none opened a correction, none reopens that review, and no correction transition is offered for that candidate. The reviewer's rationale is not carried in the closure envelope, so the locations are the whole of what was recorded. | Ordinary native review, PR-09b | F1 | open | Recorded 2026-09-17. Not actioned: none is blocking and the closure forbids re-running review on that candidate (the PR-08a precedent, B-22). Reversible — each location can be examined on its own terms in a later slice. |
| B-33 | **`test/security/provenance.test.ts` treats a file's *leading* `/**` block as a vendor header, so a non-vendored module that begins with its doc comment cannot spell that header's first token.** `leadingProvenanceHeader` inspects a block only when the file `startsWith("/**")`, and returns it when that first block contains the header's opening token (a capitalised `Provenance` with a colon); `parseHeader` must then parse it or the suite fails with `malformed Provenance header(s) in: <path>`. A module with **no imports** has its own doc comment as that block, so it cannot state "this file carries no provenance header" the ordinary way. PR-10's `src/ledger/schema.ts` — a single exported DDL string, no imports at all — hit exactly this: the focused suite was green, only `test:static` failed, and only once the file became visible to `git ls-files`. Both PR-10 modules now state the constraint without spelling the token. | PR-10's own discovery while implementing `src/ledger/schema.ts`; the gate reported the first draft as a malformed vendored module | F1 | open | Recorded 2026-09-17. The workaround is documentation, not a fix: the heuristic cannot distinguish *being* a header from *describing* one. Candidate fix for a later hygiene PR: key on the exact header prefix (`Provenance: telegram-agent-bus `) rather than the bare token, which would also sharpen the diagnosis of a genuinely mistyped header. Reversible; every earlier non-vendored module was safe only because its imports come first |
| B-34 | **The TS18003 rule is stated falsely in three places, and it is not the rule its authors thought it was.** Measured with this repository TypeScript 7.0.2 (three independent passes: PR-11 Judgment Day judge A, its judge B, and the RDD independent verifier): an **unused** project reference builds clean (`exit 0`); `error TS18003: No inputs were found in config file` is what a project whose `include` matches nothing reports **when it carries no `references` entry at all**, and any entry — even `"references": []` — suppresses it, exactly as `files: []` does; composite-ness neither causes nor prevents it. The false wording stands in `src/cli/tsconfig.json:10` (a referenced composite project with no inputs is TS18003) and `src/registry/tsconfig.json:12`, both files of slices already merged and audited, which PR-11 was therefore not allowed to edit, and it stood in the handoff §5 item 3 and §6 until PR-11 close. `src/ledger/tsconfig.json` carries the measured version. | PR-11 Judgment Day round 1 (`JD-A-006` / `JD-B-003`, both judges reached it) and its round-2 scoped re-judgment, where one judge resolved it `regression` and the other `verified` while both measured the same facts | F1 | open | Recorded 2026-09-17. Wording only, one comment per file; it should ride with whichever slice next opens those units, or with a hygiene PR, because they belong to audited slices |
| B-35 | **PT-10's assertion cell and the evidence cell PR-12 wrote read as a contradiction.** The assertion says "the confirmed offset never exceeds the last persisted `update_id`", while the writer advances the offset to `max(update_id) + 1` over **every** entry a batch covered — and a dropped entry persists no `updates` row at all, so in a batch of drops the offset sits past every persisted `update_id`. The operative reading is "the offset names no update the ledger skipped", and PR-12 states that reading in the evidence cell it owns, so the row is no longer self-contradictory; what remains open is the assertion's own wording, which is the Director's to accept as read or to reword. | PR-12 Judgment Day round 1, `JD-B-007` (one judge) | F1 | open | Recorded 2026-09-17. Wording only, in a gated threat-model row: PR-12 was not allowed to reword the assertion itself, so it reconciled the two cells and filed this row rather than editing a gate |
| B-36 | **The three advisory findings of PR-12's ordinary native review, recorded not actioned.** That review closed **APPROVED** for lineage `review-b6fc7d771f933aed` (one lens, `review-reliability`, medium risk, 10 changed files / 2,427 changed lines, authority burned), and its closure carries everything below — the statement text is not part of the closure, so this row carries exactly what it does: `R3-replay-audit-idempotency` (WARNING, `test/ledger/inbox.test.ts:144-164`), `R3-catchup-project-scope` (SUGGESTION, `test/ledger/cursors.test.ts:129-139`) and `R3-ensure-instant-order` (SUGGESTION, `src/ledger/cursors.ts:147`). All three are `informational`: none opened a correction transition and none reopens the review. | PR-12's ordinary native review, approved | F1 | open | Recorded 2026-09-18. Their ids name their subjects — the replay-audit idempotency, the catch-up test's project scoping and an instant-ordering question in `ensureClientCursor` — and the first of them is adjacent to the boundary the record already discloses (a replayed *drop* is audited again, because a dropped update leaves no `updates` row for PT-10's key to match). Precedent: **B-21** and **B-32**, the advisory sets of PR-07b's and PR-09b's approved reviews |
| B-37 | **The `ledger` spec's "No token in any ledger table" scenario names three write paths, and only two of them have a token guard.** The scenario feeds a token-shaped string through "poller admission, send-path audit, cursor advance" and asserts it never lands in a table. PR-13 pinned the first two — the shared audit writer, and through it the poll batch's audit rows, plus `unknown_senders` and `conditions` — and the third, the cursor advance (`ensureClientCursor`/`advanceClientCursor` in `src/ledger/cursors.ts`), has no guard at all: `client_id`, `host` and `last_surfaced_digest` are stored as handed over. The practical exposure is low (the IPC handshake mints `client_id`, PR-30, and the digest is computed by `shared/protocol-select.ts`), but the scenario is not satisfied for that path. **The peer-body columns are a separate and larger gap, filed as B-38** — this row covers the cursor path only. | PR-13 implementing `src/ledger/audit.ts`, reading the requirement's scenario against the paths that exist | F1 | open | Recorded 2026-09-18. Dispositions: add the guard to `ledger/cursors.ts` in whichever slice next opens that unit — a drive-by edit to an audited, frozen module is what this repository's freeze rule forbids; or narrow the requirement's scenario to the two paths a ledger writer owns and state the cursor half as the IPC handshake's contract. Reversible |
| B-38 | **The ledger's peer-body columns are unguarded, and the receive-side control the record credited them to does not exist in F1.** The `ledger` spec's "No token in any ledger table" requirement names `updates` among its tables, and design §6's `Never` row says the ledger is a place "no column receives it", while `updates.body` — and `threads.body`/`thread_history.body` by the same argument — is written exactly as received from the Bot API. PR-13's `src/ledger/audit.ts` first credited "the admission pipeline's own receive-side secret scan (`SECRET_PATTERN_DETECTED`, `daemon/admission.ts`, PR-22a)"; Judgment Day round 1 measured that no such step exists anywhere in F1: v1's `SECRET_PATTERN_DETECTED` occurs only on the **send** path (`v1:src/tools/send.ts:163,349`), `checkForSecrets` is documented as the OUTBOUND backstop (`v1:src/secrets.ts:29-34`), `grep -i secret` over `v1:src/tools/fetch.ts` is empty, design §8.2's seven-step admission table and the `durable-inbox` spec's admission requirement list no scan step, and `tasks.md`'s PR-22a block names none. Reproduced by the writer as well: `commitInboxBatch` with a token-shaped `body` stores it, and the ledger file's bytes match the shape after `PRAGMA wal_checkpoint(TRUNCATE)`. A guard **in the ledger's own writer** is the wrong fix — `updates.body` is written by the poll batch, and refusing the batch would leave the offset unmoved and the poller wedged on the same update for ever. | PR-13's Judgment Day round 1, `JD-A-001` (one judge; reproduced deterministically by the writer before any correction) | F1 | open | Recorded 2026-09-18. Dispositions: add the receive-side scan to admission (PR-22a) and drop such an update with `SECRET_PATTERN_DETECTED`, which the audit vocabulary already carries (DATA-MODEL §3.6) and which is the placement the design reserved; or narrow the requirement to the columns a governor owns and state the body columns as admission's contract; or redact the shape on write — a fourth redaction site, and the reason the ledger's writers refuse rather than redact today. Reversible; the pre-existing half is not this slice's, and the false compensation claim that was this slice's has been corrected in `src/ledger/audit.ts` and in PT-20's cell. **Update 2026-09-20: PR-22a — the slice this disposition named — closed WITHOUT adding the receive-side scan, deliberately.** design §8.2's seven-step table, the `durable-inbox` admission requirement and `tasks.md`'s PR-22a block all name seven steps and no scan, so adding an eighth step would have been a change to a gated design rather than an implementation of it; the placement is still open and the choice among the three dispositions above remains the Director's. The shipped `src/daemon/admission.ts` writes `updates.body` exactly as received and its own doc states that the peer-body columns are unguarded in F1. |
| B-39 | **CI is intermittently red because two wall-clock-sensitive daemon-lifecycle tests flake, so a red leg is not by itself evidence that a commit is broken.** Measured on `main` itself: run `35540238564` (the PR-21 merge commit) failed leg `build-and-test (24.15)` on `heartbeat: ticks at periodMs` (`test/daemon/lifecycle/heartbeat.test.ts`, `periodMs: 20`), and run `35538274064` (the PR-20 merge commit) failed the same leg on `main: starts daemon process and exits cleanly on SIGTERM` (`test/daemon/main.test.ts`, a 5-second budget) — while in both cases the immediately following documentation commit on `main` ran the same suites green (`35540401379`, `35538675419`). PR-22a met the heartbeat flake on the Node 26 leg of run `35542073670` and both legs passed on a bare `gh run rerun <id> --failed`. | observed while merging PR-22a (2026-09-20) and cross-checked against `main`'s own run history | F1 | open | Recorded 2026-09-20. Neither test takes a tolerance: the heartbeat expects a tick count at `periodMs: 20` and the SIGTERM test expects a clean exit inside five seconds, so a loaded runner decides the verdict. Dispositions: (a) replace both fixed expectations with a deadline-based wait plus an explicit tolerance, (b) serialize them (`{ concurrency: false }`) so they stop competing with the rest of the suite, or (c) state CI's flake policy instead of fixing it — re-run the failed job once and record the re-run in the PR (what PR-22a did). (c) is the cheapest and is not a fix: it leaves the gate probabilistic, and a genuinely broken commit can hide behind a re-run as easily as a flake. Reversible |
| B-40 | **Design §8.1 names two poller conditions that no gated contract defines.** The loop pseudocode says a 409 raises condition `poller_conflict` and a 429 raises `poller_rate_limited`, but `src/ledger/conditions-store.ts` accepts exactly four names (`ledger_quarantined`, `group_outage`, `state_quarantined`, `open_thread_backlog`), `shared/tool-output.ts`'s hash-pinned `Conditions` output shape has no member for either, and DATA-MODEL.md never lists them — so no scope, detail contract or `status` rendering exists to implement against. PR-22b's `src/daemon/poller.ts` raises neither; its writer's audit called them out of scope, which is not what the design says. The gated requirement (`daemon-lifecycle` › "Telegram 409 surfaced, never retried blindly") is met without them through `offsets.last_error_code`, which PR-24's `status` reads per bot. | found while repairing PR-22b's close-out (session 27, 2026-09-22) | F1 | open | Dispositions: (a) amend design §8.1 to drop the two conditions (the `offsets` columns already carry both facts to `status`); (b) define both contracts and an output member and raise them in a poller slice with its own audit. (a) is the smaller change and moves no hashed bytes. Pointer: `apply-progress.md` §PR-22b. |
| B-41 | **Design §6 says the secret-store selection raises condition `secret_store_fallback`, and nothing in the tree can hold or render it.** `src/secret-store/index.ts` exports `SECRET_STORE_FALLBACK_CONDITION` and returns it on `SecretStoreSelection`, but `src/daemon/bootstrap.ts` keeps only `selection.store` and drops the condition and its reason; `src/ledger/conditions-store.ts` accepts four names and not this one; `shared/tool-output.ts`'s hash-pinned `Conditions` shape has no member for it. Same class as B-40. PR-24's `status` reports `secret_store.kind` (`keychain` or `file`), which carries the same fact to a reader; the fallback *reason* is surfaced nowhere. | found while planning PR-24 (session 27, 2026-09-22) | F1 | open | Dispositions: (a) amend design §6 to say the fallback is visible through `status.secret_store.kind` and drop the condition; (b) define a daemon-scope contract for it in `conditions-store`, keep the selection's condition in `bootstrap.ts` and render it in `status.daemon_conditions` — a slice touching two closed units. (a) moves no hashed bytes. |
| B-42 | **A SEAM module's `v1 body sha256` header value is not machine-checked against the v1 source.** `test/security/provenance.test.ts` asserts, for a SEAM entry, only that the v2 body hash differs from the pinned value and that `Changes:` is not `none.`; any 64-hex value passes, so a wrong or stale pin in any of the 14 SEAM headers would not fail a test (ADR-12). The v1 checkout is not in the repository or in CI, which is why the ratified convention (`bus-v2-f1-pr-04-001`) verifies SEAM pins by reproduction during each slice's audit instead — PR-26's verifier reproduced `771f968e…` from the frozen checkout. Raised as CRITICAL by Judge B in `bus-v2-f1-pr-26-audit-001` and classified pre-existing (the assertion predates PR-26 and applies to every SEAM entry). | Judgment Day PR-26 (session 28, 2026-09-22) | F1 | open | Dispositions: (a) keep audit-time reproduction as the check of record and say so in `provenance.test.ts`'s doc; (b) add an opt-in test that recomputes every pin when an `AGENTBUS_V1_CHECKOUT` path is set, skipped otherwise; (c) vendor a hash-only manifest of the cited v1 ranges into the fixture so the pins are cross-checked against a second record. |
| B-43 | **Merged modules still name v1's tool prefix in runtime text or pointers.** `src/daemon/serve/thread.ts:204`'s `UNKNOWN_THREAD` message tells the agent to run `agentbus_fetch`, a tool this product does not ship (`TOOL_PREFIX` is `conmuta_`); `src/daemon/admission.ts`'s provenance note says to "see `test/fixtures/v1-provenance.json`" to reproduce a hash the fixture does not carry. PR-26 corrected both in its own module (`JD-A-003`, `JD-B-002`); the merged modules belong to closed unit 7 and are not edited drive-by. | Judgment Day PR-26 (session 28, 2026-09-22) | F1 | open | One small slice (or PR-31's IPC routes, which own the tool names) replaces the runtime string with `${TOOL_PREFIX}fetch`, pins it by a test, and corrects the comment. |
| B-44 | **A room-guard refusal raised inside the transport is reported as a group outage, not as `WRONG_ROOM`.** PR-27's send path pre-checks every target with `RoomGuardClient.assertTarget` before `transport.send`, so a guard or `config.chat_id` mismatch is refused with `WRONG_ROOM`, an audit row and zero calls. The decorator inside the transport stays the last line: if a `GroupTransport` were built against a group the guard does not hold, the decorator still refuses that post before any call — nothing reaches the wrong room — but the AS-IS, hash-pinned `GroupTransport` turns the refusal into a soft `{ok: false, code: "TRANSPORT_ERROR"}` without its cause, so the send degrades, raises `group_outage` and writes a `degraded` row. `bindings.ts`'s `buildTransport` builds the guard and the group transport from one `group_id`, so production cannot construct that state; a test in `test/daemon/send/send-path.test.ts` pins the observed behaviour. Raised by Judge B in `bus-v2-f1-pr-27-audit-001` (as a `WrongRoomError` reaching the send path's catch, which the parent's probe showed cannot happen). | Judgment Day PR-27 (session 28, 2026-09-23) | F1 | open | Dispositions: (a) accept — isolation holds and construction is single-sourced; PR-41's two-binding job exercises the real `buildTransport`; (b) a SEAM change to `GroupTransport` that keeps a `WrongRoomError` as a hard failure, reopening a hash-pinned AS-IS module (size:exception precedent PR-20). |
| B-45 | **The poller's 429 handling disagrees with DATA-MODEL and shares `offsets.retry_after_until` with the send path without coordination.** Measured in `src/daemon/poller.ts` (PR-22b, closed unit 7): (1) a 429 on `getUpdates` writes `offsets.last_error_code = 'TELEGRAM_RATE_LIMITED'`, while DATA-MODEL §3.1 lists `RATE_LIMITED` in that column's closed enum; (2) a successful poll sets `retry_after_until = NULL`, which can erase a backoff the send path recorded from a `sendMessage` 429 (PR-28) before it expires — the send path then learns the limit again from Telegram rather than locally; (3) the poller writes no audit row for a 429, although `RATE_LIMITED` is in the `audit_log.reason` enum (the 409 branch does write one). PR-28 settles the tool-facing code as `RATE_LIMITED` (spec, DATA-MODEL, THREAT-MODEL T22 and tasks agree; `TELEGRAM_RATE_LIMITED` stays the internal classification code of `daemon/telegram.ts`). | found while mapping PR-28 (session 28, 2026-09-23) | F1 | open | One slice owning `poller.ts`: write `RATE_LIMITED`; clear `retry_after_until` only when it is in the past; add the 429 audit row — each pinned by a test. Or split the column per plane (a DDL change, ADR-level). |
| B-46 | **Design §9 names the send path's rate refusal `TELEGRAM_RATE_LIMITED{retry_after_s}`; the shipped tool-facing code is `RATE_LIMITED`.** The spec (`send-path › Rate discipline without auto-retry`), DATA-MODEL's `offsets.last_error_code` and `audit_log.reason` enums, THREAT-MODEL T22/PT-33 and `tasks.md`'s PR-28 block all spell `RATE_LIMITED`; PR-28 followed them and recorded the choice in its apply-time note. `TELEGRAM_RATE_LIMITED` remains the internal classification code of `src/daemon/telegram.ts`. The design's text is gated and was left as written. Raised by Judge B in `bus-v2-f1-pr-28-audit-001`. | Judgment Day PR-28 (session 28, 2026-09-23) | F1 | open | Amend design §9's rate row to `RATE_LIMITED{retry_after_s}` at the next design touch (a text-only change), or confirm the two-name split in an ADR note. |
| B-47 | **The IPC `Host` check has no threat-model row for the client routes.** Design §10 applies the `Host` header check ("DNS-rebinding defence, applied to the client routes now and reused by the F3 panel") and PR-29 ships it with tests, but THREAT-MODEL cites DNS rebinding and `Host`/`Origin` validation only under T18 (the F3 web panel, PT-29); T14/T16 and PT-24/PT-26, the threats the client-daemon IPC defends, never mention it, and no PT id pins the body cap `IPC_MAX_BODY_BYTES` either. Found by PR-29's mapping. | PR-29 mapping (session 29, 2026-09-23) | F1 | open | Add the `Host` check and the body cap to T14's mitigation text (or a new PT row) at the next THREAT-MODEL touch, citing `test/daemon/ipc/server.test.ts`. |
| B-48 | **Design §15's PT map pins PT-24 to `daemon/ipc/server` and PT-26 to `client/handshake`, which looks stale against the later `ipc/{handshake,sessions,routes}` split.** `daemon/ipc/server.ts` (PR-29) has no bearer/rotation logic at all per its own module doc ("never reads or checks Authorization"); the actual bearer-rotation behaviour PT-24 describes lives in `daemon/ipc/sessions.ts` (PR-30). PT-26's `client/handshake` pin is directionally right (clauses (a)/(c) are PR-33's) but names only one of the two modules that jointly satisfy it once `daemon/ipc/handshake.ts` (PR-30) exists. Found while mapping PR-30. | PR-30 mapping (session 30, 2026-09-23) | F1 | open | Update design §15's PT-24/PT-26 file pins at the next design touch (text-only) once PR-30/31/33 land, e.g. PT-24 → `daemon/ipc/sessions`, PT-26 → `daemon/ipc/handshake` + `client/handshake`. |
| B-49 | **spec.md's "Binding never changes mid-session" scenario reads as if the whole resolved binding (roster included) stays frozen, while design.md's Freeze row and the shipped code freeze only three identity fields (`bot_id`, `group_id`, `agent_id`) and deliberately let `roster_snapshot`/`roster_hash`/`config` update live mid-session.** This is coherent with the separate roster-drift requirement and is explicitly reasoned in `daemon/ipc/routes.ts`'s own module doc (`FrozenBindingSnapshot`, narrower than `bindings.ts`'s `areBindingsEquivalent` on purpose), so it is not a code defect — but spec.md's own wording was not updated to make the narrower scope explicit and could mislead a future reader. Raised by Judge A in `bus-v2-f1-pr-31-audit-001`. | Judgment Day PR-31 (session 31, 2026-09-23) | F1 | open | Tighten `specs/ipc-handshake/spec.md`'s "Binding never changes mid-session" scenario wording to name the three frozen fields explicitly, at the next spec touch (text-only). |
| B-50 | **`daemon/ipc/routes.ts`'s `dispatchTool` forwards a caught tool-level error's raw `.message` into the external-facing HTTP response body with no redaction pass at this boundary**, unlike every egress site design.md's Redaction table names (`daemon/telegram.ts` error constructors, `daemon/log.ts`, every audit/condition writer) — `routes.ts`/`dispatchTool` is the first site forwarding arbitrary caught-error text straight to an external MCP caller and is absent from that table. No concrete leak is demonstrated (the known error types on this exact path — `SendToolError`/`ThreadToolError` static messages, self-redacting `TelegramError` subclasses — carry no secrets, and `shared/secrets.ts` only detects, it does not redact), so this is defense-in-depth, not a proven vulnerability — matches this project's own **B-37**/**B-38** precedent for the identical class of gap (no receive-side token scan in F1). Raised by Judge B in `bus-v2-f1-pr-31-audit-001`. | Judgment Day PR-31 (session 31, 2026-09-23) | F1 | open | Design a shared redaction primitive (none exists yet; `shared/secrets.ts` is detection-only) and apply it at `dispatchTool`'s response boundary, in a dedicated slice — not an opportunistic edit inside an unrelated PR. |
| B-52 | **`shared/ipc-contract.ts`'s `toolSuccessSchema` (`z.record(z.string(), z.unknown())`) and `ipcErrorSchema` are not cross-validated against each other at the point `client/ipc-stub.ts`'s `IpcSession.callTool` classifies a response** — the classification branches solely on the HTTP status (`res.ok`), then parses the body against exactly one of the two schemas. `toolSuccessSchema`'s deliberately bare "some JSON object" shape would also accept a body shaped exactly like an `ipcErrorSchema` payload (`{code, message, retryable}`), so a `200` response carrying an error-shaped body would be silently reported to the calling agent as a successful tool result with no independent defense. Not currently exploitable — `daemon/ipc/routes.ts`'s `dispatchTool` never pairs `HTTP_OK` with anything but real tool output today — but the client adds no defense-in-depth of its own should that daemon-side contract ever be violated by a future change to either module. Raised by Judge A and, independently, the RDD fallback's independent verifier (as a `z.strictObject` forward-compatibility fragility) in `bus-v2-f1-pr-34-audit-001`. | Judgment Day PR-34 (session 34, 2026-09-24) | F1 | open | Cross-validate the body against both schemas (or add a discriminant field to the wire contract) before trusting the status-code-only classification, in a dedicated slice touching `shared/ipc-contract.ts` — not an opportunistic edit inside an unrelated PR. |
| B-51 | **`client/handshake.ts`'s `GET /identity` and `POST /session` both reuse `IPC_REQUEST_TIMEOUT_MS` (70 s, sized in `constants.ts` for the daemon's long-poll fetch tool call) as their own `AbortSignal.timeout`**, and the identity step can attempt this twice (initial + one re-read retry) before raising `DAEMON_DOWN` — so a daemon whose process is alive and whose port accepts the TCP connection but that stalls before answering (as opposed to a clean refusal, which fails fast) could block the very first tool call for up to roughly two minutes, in tension with design.md §11's "Handshake timing" intent that a down daemon reads as a fast tool error, not a hang. Not a correctness bug — the independent verifier's own standalone probe confirmed `AbortSignal.timeout` fires and is caught cleanly, folding into `DAEMON_DOWN` exactly as designed — purely a worst-case latency concern on an uncommon transient state. Raised by Judge A in `bus-v2-f1-pr-33-audit-001`. | Judgment Day PR-33 (session 33, 2026-09-24) | F1 | open | Introduce a dedicated, reasoned `IPC_HANDSHAKE_TIMEOUT_MS` constant (much shorter than the long-poll timeout — this is a loopback round-trip to a process already confirmed alive via `ensureDaemonRunning`) and use it in `requestIdentity`/`requestSession` instead of `IPC_REQUEST_TIMEOUT_MS`, in a dedicated slice. |
| B-53 | **`client/main.ts`'s `runMcpClient` cannot supply the real MCP host-application label (`host`, e.g. `claude-code`/`cursor`/`opencode` — DATA-MODEL.md §3.5's `client_cursors.host` row; `shared/ipc-contract.ts`'s `IPC_SESSION_HOST_MAX_CHARS` doc comment) because `IpcSession` must be constructed, and the daemon session identity fixed, BEFORE `server.connect(transport)` ever runs the MCP `initialize` exchange that would reveal the connecting host's `clientInfo.name`.** PR-35's original candidate used `os.hostname()` (the machine name — wrong vocabulary entirely, and long enough on some hosts to exceed the 64-char field cap and fail the handshake outright); corrected during Judgment Day (Judge B CRITICAL) to a disclosed fixed placeholder (`MCP_HOST_LABEL_UNKNOWN = "unknown"`). Properly wiring the real label means either deferring `IpcSession`/`createServer` construction until after `initialize` (touches `client/ipc-stub.ts` and `client/server.ts`, both already-merged PR-34 modules) or adding an `oninitialized`-style callback path to update the identity post-construction — a real design change, not a drive-by fix. Raised by Judge B in `bus-v2-f1-pr-35-audit-001`. The fixed placeholder also means every session now looks identical in `client_cursors.host`, a real (non-exploitable — `daemon/ipc/sessions.ts` never keys or dedupes on `host`) reduction in per-machine operator debuggability versus the wrong-but-varied `os.hostname()` value it replaced, raised by Judge B again in the scoped re-judgment round. | Judgment Day PR-35 (session 35, 2026-09-24) | F1 | open | A dedicated slice restructures `client/ipc-stub.ts`/`client/server.ts` (or adds a narrow post-`initialize` identity-update seam) so `host` carries the MCP SDK's real negotiated `clientInfo.name`, replacing the disclosed `"unknown"` placeholder. |
| B-54 | **`daemon/bootstrap.ts`'s own `openLedger(...)` call site never checks `LedgerOpenResult.status` for `"quarantined"` either — the same blind spot PR-37's Judgment Day found and fixed in `migration/main.ts`.** Judge B raised it as a PR-37-introduced CRITICAL; Judge A independently found the identical pattern already in this already-merged `bootstrap.ts` and, on that basis, treated it as pre-existing/shared rather than unique to PR-37, declining to report it there. The Director's call for PR-37: fix `migration/main.ts` (a one-shot historical-data import silently discarding visibility into pre-existing ledger data is worse than the daemon's own ongoing-operation case) and file this row for `bootstrap.ts` separately rather than touch an already-merged, already-audited file from an unrelated PR. Consequence if a daemon boot ever quarantines its own ledger: the daemon silently starts against a fresh empty database with no operator-visible warning beyond whatever `status`/`gentle-ai review` surfaces after the fact. | Judgment Day PR-37 (session 37, 2026-09-25) | F1 | open | A dedicated slice adds the same `status === "quarantined"` check (and an operator-visible warning, or a refusal matching `migration/main.ts`'s own choice) to `daemon/bootstrap.ts`'s ledger-open call site. |
| B-55 | **`design.md`:519 (section 14, "Static security assertions") cites the v1 source range for `test/security/predicates.ts` as `test/security.test.ts:44-101`, verdict `AS-IS` — superseded by the ratified tribunal ruling `bus-v2-f1-tasks-001` item 2 (`docs/05-tribunal/INDEX.md:191`), which closed at the tasks-phase audit, chronologically after the design-phase audit in the same original session, and fixes it at `25-101`, verdict `SEAM`.** The shipped code (PR-39) correctly follows the ratified `25-101`/`SEAM` reading (internally consistent with `tasks.md`'s own ~77-line estimate; `44-101` gives 58); `design.md`'s own text was never swept to match after that ruling. Found while implementing PR-39, before writing any code; confirmed with Alpha (`bus-v2-f1-pr-39-audit-001`). Disclosed as an apply-time note under `tasks.md`'s PR-39 block, not a rewrite of either gated document. | PR-39 (session 39, 2026-09-25) | F1 | open | Update `design.md` section 14's `predicates.ts` citation to `test/security.test.ts:25-101`, verdict `SEAM`, at the next `design.md` touch (text-only). |
| B-56 | **`design.md` section 14's static-assertion table (lines 517-533) has three stale claims against the actually-shipped code, found while writing `test/security/{client-bundle,daemon-bundle}.test.ts` (PR-40b) against the real, now-complete daemon closure (PR-40a).** (1) The table's illustrative `child_process` shape names a bare `spawn(...)` call; `client/spawn.ts`'s real, disclosed shape is `spawnImpl(process.execPath, [DAEMON_ENTRY], SPAWN_OPTIONS)` — an injectable indirection over `child_process.spawn`, documented in that file's own module doc as an apply-time refinement, not a regression. (2) The table's "unbounded loop" row implicitly assumes `predicates.ts`'s shared `hasUnboundedLoopReference` (v1's `for(;;)`/`while(true|1|!0)` shapes) matches the poller's real loop; `daemon/poller.ts`'s actual shape is `while (!signal.aborted)`, which that predicate does not match at all — a real, disclosed blind spot in the shared (already SEAM-audited, PR-39) predicate, not fixed here since `predicates.ts` was out of PR-40b's scope. (3) The table's `sendMessage` row names only the three AS-IS transports (`transport/{group,direct,room-guard}.ts`); `daemon/send/rate.ts`'s `RateLimitRecorder` (PR-28, after §14 was written) is a fourth legitimate call site, wired below `RoomGuardClient` in `bindings.ts`'s `buildTransport` so it never bypasses the guard. All three are pinned against the real, verified code in `test/security/daemon-bundle.test.ts`'s own assertions and module-doc disclosures rather than silently forcing a false claim to pass; none is a production defect. | PR-40b (session 40, 2026-09-26) | F1 | open | Update `design.md` section 14's illustrative `child_process`/unbounded-loop text and the `sendMessage` transport list to match the shipped shapes, at the next `design.md` touch (text-only); optionally extend `predicates.ts`'s `hasUnboundedLoopReference` to also catch an `AbortSignal`-driven `while (!x.aborted)` shape, in its own dedicated slice (touches an already SEAM-audited module). |
