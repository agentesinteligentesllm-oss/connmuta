# Backlog checklist — items to land gradually

> Purpose: one row per item the Director or the tribunal has deferred, so nothing is lost and
> nothing is re-debated. Each item is processed in its own session or debate, in the order the
> Director decides. A row is closed only with a pointer (debate id, ADR, commit or Director note).
>
> Rules: one item per row; never delete a row, mark it `done` or `dropped` with the pointer;
> the phase column follows the work plan agreed in debate `bus-v2-landing-architecture-001`
> (F0 landing … F6 packaging; F7+ are post-v1 tracks).

Status legend: `open` · `in-debate` · `decided` · `done` · `dropped`

| # | Item | Origin | Phase | Status | Pointer |
|---|------|--------|-------|--------|---------|
| B-01 | **Group referee role** (Director: "árbitro / coordinador" per Telegram group). One optional referee per group, with an explicit rule set: registers tickets with mandatory labels (raising agent, date, status, repo version vs local/VPS version), homologates state, blocks duplicates, re-work and re-debate. Precedent: `FRISCO/frisco-erp/coordinador` (mechanical gate, writes nothing, authorizes nothing, own ledger outside the tree). Runs as a satellite package, never inside the bus core (Alpha objection n1, ADR-06). | Director 2026-09-15 | F7 | open | dedicated debate `bus-v2-referee-001` |
| B-02 | **Skill templates** shipped with the product: Markdown prompts (referee, auditor, developer, documenter…) placed where the control panel can offer them per bot/group for one-click adoption by any agent host. | Director 2026-09-15 | F7 | open | same debate as B-01 |
| B-03 | **Ticket ledger location**: daemon-owned SQLite table exposed as MCP tools (`ticket_open`, `ticket_find`, `ticket_status`) vs external adapters (Odoo, GitHub Issues, Linear). Decide after B-01. | Director 2026-09-15 | F7 | open | — |
| B-04 | **Desktop shell** (system-tray app) for Windows first, macOS second, wrapping the SAME daemon and the SAME local web panel (no second UI). Candidates: Tauri v2 (small, native tray, autostart plugin, Node daemon as sidecar, needs Rust toolchain) vs Electron (JS-only, heavier). Signing budget: Azure Trusted Signing + Apple Developer ID. | Director 2026-09-15 | F8 | open | amendment A2 in `bus-v2-landing-architecture-001` |
| B-05 | **Study gentle-ai installer** (`Gentleman-Programming/gentle-ai`, Go binary, `install --agent <list> --scope global\|workspace`) in a dedicated exploration session: its per-agent adapters (detection paths, config merge semantics, TUI flow). Goal: validate/complete our 15-tool MCP config matrix, reuse knowledge, not code. | Director 2026-09-15 | F0 spike | open | — |
| B-06 | **Headless runner satellite package** (invokes `claude -p`, `codex exec`, `opencode run`, `gemini -p` on new `needs_action`). Out of the core by ADR-06 layers 1–2; own constitution and package; read/reply-only default. First consumer: B-01. | Alpha objection n1 | post-F6 | decided | `bus-v2-landing-architecture-001` round 2 |
| B-07 | **Spike**: bot-to-bot group visibility for a NON-admin bot with privacy disabled — AND vs OR semantics (two official Telegram pages disagree). Determines whether project bots must ever be admins. | critic gap | F0 | open | — |
| B-08 | **Spike**: IPC handshake on Windows 11 — challenge-response over the per-boot secret before any bearer is sent (Alpha objection n2), plus whether a Node-created named pipe is reachable by another local user. | Alpha objection n2 | F0 | open | — |
| B-09 | **Spike**: per host (Cursor, OpenCode, Codex, Gemini CLI, Antigravity, Claude Code) whether any MCP server-initiated notification reaches the model or triggers a turn. Decides what "timely attention" can honestly promise. | critic gap | F0 | open | — |
| B-10 | **N-party tribunal over the bus** (proposer + judges on different models): needs AGENTBUS/3 or Arena Orion integration; v1 ships 2-party Arena-light only. | proposal D7 | post-F6 | open | — |
| B-11 | **Name clearance**: IMPI + EUIPO screening for "Conmuta" (collision: Conmuta Soluciones Tecnológicas S.L., class 42); fallback "Emisario". Reserve npm scope, GitHub org and domain the same day the Director decides. | proposal D8 | F0 | open | Name decided: Conmuta (DN-02, 2026-09-16); screening and reservations still open |
| B-12 | **macOS smoke test** (installer + daemon + LaunchAgent + one IDE) before claiming dual-platform support. | proposal D9 | F6 | open | — |
| B-13 | **Migration runbook from `~/.agentbus`** (FRISCO team on v1.0.1/v1.0.2): backup, synthesized registry, wire-compat policy (emit AGENTBUS/2, accept /1 and /2). | proposal D1 | F1 | decided | design.md §13, specs `v1-migration`, tasks PR-36..PR-38 (`bus-v2-f1-design-001`); `done` when PR-38 merges |
| B-14 | **Version observability without autonomous emission**: no heartbeat timer (ADR-06 layer 2); build/wire version rides on the render-only header of posts the agent sends anyway. | Kairo self-correction | F3 | decided | amendment A1 |
| B-15 | **Secrets storage verification**: `@napi-rs/keyring` prebuilds on win-x64 and darwin-arm64; fallback file ACL (`icacls` / `chmod 600`) with tests on both platforms. | proposal D4 | F1 | decided | design.md §6 (D-18), specs `secret-store`, tasks PR-14 (`bus-v2-f1-design-001`); win-x64 `done` when PR-14 merges, darwin half stays B-12 |
| B-16 | **Repository hygiene for open source**: LICENSE (Apache-2.0, Director to confirm), SECURITY.md (five invariants), CONTRIBUTING.md, CHANGELOG.md, secret scan in release checklist. | proposal D10 | F0 | open | LICENSE done (Apache-2.0, DN-04, 2026-09-16); SECURITY/CONTRIBUTING/CHANGELOG and the copyright-holder line still open. PT-22 (`test/security/repo-scan.test.ts`, PR-01a) proves the mechanism with synthetic markers only — a real tenant deny-list cannot live in a public tree and must come from outside it (CI secret); decide at the PR-42 close-out (`bus-v2-f1-pr-01-001` item 7) |
| B-17 | **Node ≥ 24 gate** as the first check of the installer, before touching disk or git, with an explicit error and download link (Alpha objection n3). | Alpha objection n3 | F2 | decided | `bus-v2-landing-architecture-001` round 2 |
| B-18 | **Committed project file** `conmuta.json` at repository root, strict zod schema, identifiers only, no local paths, `schema_version` (Alpha objection n4). File name follows the final product name. | Alpha objection n4 | F1 | decided | `bus-v2-landing-architecture-001` round 2 |
| B-19 | **`shared/constants.ts` pins a blind-stripped hash**: its header carries `4ce5e514…` for `v1:src/config.ts:26-166`, where the rule (`bus-v2-f1-pr-04-001`) requires the value **with** the terminating newline, `039d53a2…`. No gate can see it: for a SEAM the provenance scanner asserts only *inequality* against the pin. | PR-07a Judgment Day audit, re-confirmed by PR-07b's (never debated; `bus-v2-f1-pr-07a-audit-001`) | F1 | done | **Closed 2026-09-17** by the post-merge integrity sweep under the Director's session-10 delegation: `constants.ts:3` re-pinned to `039d53a2…` with `4ce5e514…` recorded as the strip control, PR-04's wrong-value-control conclusion corrected in place and its frozen S1 row left as recorded with a closure note. Two methods, validated against the ratified fence pin. See `apply-progress.md` §B-19 and `bus-v2-f1-b19-repin-001` |
| B-20 | **design §12's reuse table marks line-range extracts AS-IS over modules that are SEAM by construction**: rows 462/467 (`send.ts:47-109`, `index.ts:29-42` → `shared/tool-schemas.ts`, reported by PR-07a) and row 458 (`fetch.ts:65-348` → `shared/tool-output.ts`, reported by PR-07b). The tribunal ruling `bus-v2-f1-tasks-001` items 1–2 settles that such a module is SEAM, and the registry proves it independently — an AS-IS body must hash *equal* to its pin, which a re-authored header, an `export` or an added declaration makes impossible. | PR-07a and PR-07b Judgment Day audits (`bus-v2-f1-pr-07a-audit-001`, `bus-v2-f1-pr-07b-audit-001`) | F1 | done | **Closed 2026-09-17** by an *appended* apply-time amendment in design §12 (the audited rows are untouched, so both what was designed and what was shipped remain on the record), applied under the Director's session-10 delegation. Authority: `bus-v2-f1-tasks-001` items 1–2 + `tasks.md` 7a.2/7b.2. Revertible: the amendment note is the only part to revert if the Director prefers to re-open the mapping |
| B-21 | **Advisory finding `R3-tool-output-shape`** from the ordinary native review that closed PR-07b's candidate (lineage `review-7a57283629321227`, lens `review-reliability`): severity WARNING, disposition **informational**, located at `test/shared/tool-output.test.ts:119-121`, the declared-key-set pin for `FetchToolOutput`. The review's closure declared every finding it listed non-blocking — none opened a correction, none reopens that review, and no correction transition was offered for that candidate. The reviewer's rationale is not carried in the closure envelope, so the location is the whole of what was recorded. | Ordinary native review, PR-07b (`gentle-ai.review-acknowledged/v1`, authority burned) | F1 | decided | **Examined 2026-09-17, no change applied.** The pin at 119-121 is the *declared* eighteen-key set, which a runtime fixture cannot witness; the **mandatory** fifteen already have a runtime witness two cases below (`mandatory.length === 15` and `Object.keys(FULL_OUTPUT)`, lines 190-191), which fails if the fixture's keys drift. The two are complements, not a gap, so adding a third witness would be duplication — and the closure forbids re-running review on that candidate. Reversible if the Director reads the finding differently |
| B-22 | **Four advisory findings from the ordinary native review that closed PR-08a's candidate** (lineage `review-f644a39f445a2a0c`, lens `review-reliability`, risk tier medium, authority burned): `R3-01` WARNING at `src/shared/project-file.ts:294-296`; `R3-02` WARNING at `test/shared/project-file.test.ts:5-9`; `R3-03` SUGGESTION at `test/shared/project-file.test.ts:352-362`; `R3-04` SUGGESTION at `src/shared/project-file.ts:151-158`. The closure's own text declares every finding it listed non-blocking: none opened a correction, none reopens that review, and no correction transition is offered for that candidate. The reviewer's rationale is not carried in the closure envelope, so the locations are the whole of what was recorded. | Ordinary native review, PR-08a (`gentle-ai.review-acknowledged/v1`, authority burned) | F1 | open | Recorded 2026-09-17. Not actioned: none is blocking and the closure forbids re-running review on that candidate. Reversible — each location can be examined on its own terms and corrected in a later slice if the finding holds; the review outcome is informational and never grants or withholds delivery |
| B-23 | **`JD-A-003` (Judgment Day SUGGESTION, PR-08a): the deliberate precedence of `unsupported_schema_version` over the content walk is unpinned.** A future-version document that also carries a forbidden shape reports only the version problem, so an operator fixing an upgrade learns nothing about the leak in the same file. The file is still refused, so no secret escapes — the gap is information only. The judge offered two dispositions: report both problem classes, or pin the deliberate precedence with a test that can fail (ADR-12). | PR-08a Judgment Day audit, `jd-judge-a` informational row — **not** part of the authorized severe batch, and not among the four rows the Director folded | F1 | open | Recorded 2026-09-17 in `apply-progress.md` §PR-08a. The behaviour is already documented in `parseProjectFile`'s own doc comment; what is missing is a test that fails if it ever changes. Reversible: either disposition is one small change, and neither reopens PR-08a |
| B-24 | **No POSIX CI leg, so packaging and execution contracts cannot fail here.** `.github/workflows/ci.yml` runs `windows-latest` only, where npm's shim invokes `node` explicitly; a `bin` shebang, file modes and bin-links are therefore invisible to every gate. PR-08b's `JD-B-001` (CRITICAL) was exactly that defect in `src/cli/main.ts`, and ADR-0012 records its failure mode as a silent zero-output success that a pre-commit hook reads as "clean". | PR-08b Judgment Day audit (`JD-B-001`, CRITICAL; ADR-0012 remediation row 2) | F1 | open | Recorded 2026-09-17. The slice added a targeted assertion over the built bundle, which pins the shebang itself; what remains open is the general class. Candidate fix: a POSIX matrix entry, best decided together with the pending `actions/*` major bump. Reversible and cheap |
| B-25 | **Three cheap gates/hygiene items, all for one later audited PR**: (a) **no gate validates the YAML in this tree** — a broken `state.yaml` stayed unparseable for several sessions while `gentle-ai sdd-status` kept working, so a validity check over `git ls-files '*.y*ml'` (session 11 parsed it with PyYAML by hand) would close the class; (b) `npm test` runs whatever is in a stale `dist/`, so a `clean` step would stop a stale build faking results; (c) `actions/checkout@v4` and `actions/setup-node@v4` target Node 20 and emit a deprecation warning. | Hygiene candidates recorded across sessions 9–11; none is a defect report | F1 | open | Recorded 2026-09-17. Deliberately grouped: each is a few lines, each wants the same audited CI/tooling PR, and none belongs inside a feature slice. Reversible and cheap |
| B-26 | **PT-25's owner is attributed differently by two gated documents.** `tasks.md`'s PR-09 block lists `project-binding › Machine registry schema and invariants` **(PT-18, PT-25)** and task 9.6 tells PR-09 to fill PT-25's cell, while `design.md:551` (the PT-to-file mapping) sends **PT-25 to `daemon/send/send-path`**. Both readings are defensible — the row's assertion ("binding is never rewritten from bus or API data") is registry-side R6 *and* the daemon-side `GroupMigratedError` behaviour — and the same class of over-claim cost PR-06a a reverted PT-14 cell. | Found during the session-11 coherence pass over `HANDOFF.md` (reported, not silently resolved) | F1 | open | Recorded 2026-09-17. The next slice fills only the half it really pins and says so in its PR; the remaining half goes to the daemon slice. Reversible: either document can be amended by an appended note, the way B-20 was. **Apply-time note (2026-09-17):** PR-09a filled only **PT-18**'s cell and left PT-25's alone, because the half it pins is the loader's; **PR-09b** names that half (the loader exposes no write path and never renames the human's file) and states the split with `daemon/send/send-path` in its PR, per this row's own prescription |
| B-27 | **The shared token regex matches this project's own roster hash, and R5's raw-text scan therefore refuses every valid registry.** `TELEGRAM_BOT_TOKEN_RE` is `\d+:[A-Za-z0-9_-]{35}` with an **unbounded** digit run, and `ROSTER_HASH_PREFIX` is `sha256:` — a prefix that ends in a digit run — so a canonical `sha256:<64 hex>` value matches the token shape by accident. Design §4 requires the registry's R5 scan to run over the raw text *before* parsing, and `roster_hash` is required on every binding: the two together would refuse the machine registry of every correctly configured machine. PR-09b masks that one value before scanning (`sha256:` plus 64 hexadecimal characters, a region a token cannot occupy because its own colon is outside the hexadecimal class) and pins both halves with tests. | Discovered by PR-09a while writing PR-09b's loader tests (the contradiction only becomes visible once both modules exist) | F1 | open | Recorded 2026-09-17. The mask is a **workaround at the call site**, not a fix: the root cause is the v1-inherited unbounded `\d+`, which also reaches `checkForSecrets` (the outbound backstop would refuse a body quoting a roster hash) and `redactTokenShapes` (PR-14 would redact one in a log). Bounding the digit run to PT-22's `\d{8,10}` is *not* available as a silent fix: `test/shared/token-shape.test.ts` deliberately pins a **7-digit** fixture as a match, because the shared regex is the v1 backstop whose job is to over-reject. So this needs its own decision (widen PT-22's strict shape everywhere, or give the hash a prefix that cannot be read as a token — a stored-format change owned by `design.md` §2 and DATA-MODEL §2.4). Also carries the R5 half: the loader cannot name *which* rule fired without either parsing `assertNoTokenShape`'s message prose or re-implementing the shape order, so naming it is part of this decision. Reversible |
| B-28 | **No R1–R6 row demands referential integrity, so a dangling registry reference loads.** `bindings[].bot_id`, `group_id` and `project_id` name entries of `bots[]`, `groups[]` and `projects[]` (DATA-MODEL §2.4 records those as their source of truth), but no invariant row requires the entry to exist, and `parseRegistryDocument` therefore accepts a binding whose bot or group is absent. The daemon fails closed later — a binding with no `bots[]` entry has no token to resolve, and the room guard compares against `conmuta.json` (R4, at `POST /session`) — but an operator gets no signal at load time. PR-09a pins the boundary with a test instead of inventing a rule the gate does not carry; adding one would make a hand-edited file invalid for a reason no gated document states. | Discovery recorded while implementing PR-09a's `invariants.ts` against DATA-MODEL §2.4 and the R1–R6 table (design §4) | F1 | open | Recorded 2026-09-17. Owner: F2's `doctor` is the natural home — a membership check belongs where the machine is inspected, not where the file is parsed for the daemon. Reversible: if the Director prefers a load-time refusal, it is one `superRefine` rule plus its own row in the invariant table, which would be a design amendment rather than an apply-time edit |
| B-29 | **Nothing at load time ties `roster_hash` to the snapshot it is derived from.** DATA-MODEL §2.4 records the field as *derived* from `bindings[].roster_snapshot`, and design §4 says the client sends its own computation at `POST /session` so a mismatch raises `roster_drift` — but no R1–R6 row requires the stored value to describe the roster the binding admits from, and `registry/schema.ts` checks only its `sha256:<64 hex>` shape. A hand-edited registry (the R6-sanctioned path until F2) can therefore persist a fingerprint belonging to no roster and load clean, and that unverified value is what the daemon returns as `binding.roster_hash` in the session response while the drift comparison the spec defines is derived from the snapshot. Both judges reached this independently in PR-09a's Judgment Day round 1 (`JD-A-001`, `JD-B-002`); the slice fixed the fixture that made it visible (it had pinned the two-entry known answer into a one-entry snapshot) and derives the field with `computeRosterHash` so a fixture cannot disagree with its own snapshot, but the boundary itself is unchanged. | PR-09a Judgment Day round 1, two independent rows | F1 | open | Recorded 2026-09-17. Two dispositions, one decision: a load-time consistency check (which is a *new* invariant rule the gate does not carry, so it needs its own row in the R-table and an amendment), or a session handshake that recomputes the hash from the snapshot instead of trusting the stored field (a `daemon/serve` change, no schema change). The second is smaller and removes the dead-but-trusted value rather than validating it. Reversible |
| B-30 | **R5's strictness refuses human-authored text in the machine registry.** The loader runs `assertNoTokenShape` over the raw file — the whole shared secret table plus the case-insensitive `Authorization` literal — while `registry.json` carries free-form values a human writes: a group `title` of `Authorization review` or `Budget API_KEY=123` refuses the **whole** registry, and the daemon then activates no binding until the text is edited. R5's own wording is narrower ("no token-shaped string anywhere in the file"), but `shared/token-shape.ts` deliberately designates the stricter API for this call site, and this slice's Judgment Day round 1 found the *cost* on a human-editable file (`JD-A-003`, WARNING, single judge). | PR-09b Judgment Day round 1 | F1 | open | Recorded 2026-09-17. Three dispositions, one decision: scan with the narrower token-shape rules for this file; keep the strictness and surface it where the human will see it (the F2 `doctor` and the migration runbook, which already tells operators they may hand-edit the registry); or scope the literal/`.env` rules to identifier-like fields (`token_ref.account`, paths) instead of every string. The daemon's refusal is fail-closed either way, so this is an availability and diagnosis question, not a leak. Reversible |
| B-31 | **R5's raw-text scan is bypassed by a JSON escape.** The scan runs over the raw text *before* parsing (design §4), so a token written as `1234567\u003aAAHk…` has no literal `\d+:[A-Za-z0-9_-]{35}` in the file and is accepted — while the parsed document's value holds the token, which then sits in `~/.conmuta/registry.json` and in the daemon's memory and is reported by nothing. PR-09b's Judgment Day round 1 reached it with a reproduction (`JD-B-006`, SUGGESTION). | PR-09b Judgment Day round 1 | F1 | open | Recorded 2026-09-17. Dispositions: accept the raw-text approximation and say so (the escape only appears if an operator or a tool writes it that way), or add a post-parse value walk beside the raw scan — which is what `shared/project-file.ts` already does for `conmuta.json`, so the machinery exists and the vocabulary would need no new member. The second is more code in the loader; the first leaves a token that nothing reports. Reversible |
| B-32 | **Four advisory findings from the ordinary native review that closed PR-09b's candidate** (lineage `review-c5a6b9c191154861`, lens `review-reliability`, risk tier medium, authority burned): `R3-1` SUGGESTION at `src/registry/loader.ts:109-113`; `R3-2` SUGGESTION at `src/registry/loader.ts:221-229`; `R3-3` SUGGESTION at `src/registry/loader.ts:241-244`; `R3-4` SUGGESTION at `test/registry/loader.test.ts:184-185`. The closure's own text declares every finding non-blocking: none opened a correction, none reopens that review, and no correction transition is offered for that candidate. The reviewer's rationale is not carried in the closure envelope, so the locations are the whole of what was recorded. | Ordinary native review, PR-09b | F1 | open | Recorded 2026-09-17. Not actioned: none is blocking and the closure forbids re-running review on that candidate (the PR-08a precedent, B-22). Reversible — each location can be examined on its own terms in a later slice. |
| B-33 | **`test/security/provenance.test.ts` treats a file's *leading* `/**` block as a vendor header, so a non-vendored module that begins with its doc comment cannot spell that header's first token.** `leadingProvenanceHeader` inspects a block only when the file `startsWith("/**")`, and returns it when that first block contains the header's opening token (a capitalised `Provenance` with a colon); `parseHeader` must then parse it or the suite fails with `malformed Provenance header(s) in: <path>`. A module with **no imports** has its own doc comment as that block, so it cannot state "this file carries no provenance header" the ordinary way. PR-10's `src/ledger/schema.ts` — a single exported DDL string, no imports at all — hit exactly this: the focused suite was green, only `test:static` failed, and only once the file became visible to `git ls-files`. Both PR-10 modules now state the constraint without spelling the token. | PR-10's own discovery while implementing `src/ledger/schema.ts`; the gate reported the first draft as a malformed vendored module | F1 | open | Recorded 2026-09-17. The workaround is documentation, not a fix: the heuristic cannot distinguish *being* a header from *describing* one. Candidate fix for a later hygiene PR: key on the exact header prefix (`Provenance: telegram-agent-bus `) rather than the bare token, which would also sharpen the diagnosis of a genuinely mistyped header. Reversible; every earlier non-vendored module was safe only because its imports come first |
| B-34 | **The TS18003 rule is stated falsely in three places, and it is not the rule its authors thought it was.** Measured with this repository TypeScript 7.0.2 (three independent passes: PR-11 Judgment Day judge A, its judge B, and the RDD independent verifier): an **unused** project reference builds clean (`exit 0`); `error TS18003: No inputs were found in config file` is what a project whose `include` matches nothing reports **when it carries no `references` entry at all**, and any entry — even `"references": []` — suppresses it, exactly as `files: []` does; composite-ness neither causes nor prevents it. The false wording stands in `src/cli/tsconfig.json:10` (a referenced composite project with no inputs is TS18003) and `src/registry/tsconfig.json:12`, both files of slices already merged and audited, which PR-11 was therefore not allowed to edit, and it stood in the handoff §5 item 3 and §6 until PR-11 close. `src/ledger/tsconfig.json` carries the measured version. | PR-11 Judgment Day round 1 (`JD-A-006` / `JD-B-003`, both judges reached it) and its round-2 scoped re-judgment, where one judge resolved it `regression` and the other `verified` while both measured the same facts | F1 | open | Recorded 2026-09-17. Wording only, one comment per file; it should ride with whichever slice next opens those units, or with a hygiene PR, because they belong to audited slices |
