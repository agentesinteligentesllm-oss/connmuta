export const b =
  "decoy - a correct walker must never reach this file; it exists only to prove that a relative " +
  "specifier resolves against the referencing file's own directory (sub/), not the entry file's " +
  "directory (this directory), when the two differ.";
